import 'package:flutter/material.dart';

class SeedTrade extends StatefulWidget {
  const SeedTrade({super.key});

  @override
  State<SeedTrade> createState() => _SeedTradeState();
}

class _SeedTradeState extends State<SeedTrade> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
